<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Tema de la ventana. Los colores de la interfaz los define Compose en
        ui/theme/Theme.kt, asi que aqui solo se declara lo que necesita el
        framework antes de que Compose tome el control.
    -->
    <style name="Theme.MateKids" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="android:windowBackground">@color/md_theme_background</item>
        <item name="android:statusBarColor">@color/md_theme_primary</item>
    </style>
</resources>
